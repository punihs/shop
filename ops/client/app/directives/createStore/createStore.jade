.modal-header
  .modal-title
    .h4.text-info Create Category
      .close(ng-click='$ctrl.$uibModalInstance.dismiss("close")') ×
div.m-t-xl.no-margin
  .hbox
    .wrapper-md.bg-white-only
      form.form.form-validation.form-horizontal.wrapper-md(
      name="newStoreForm"
      novalidate
      ng-submit='$ctrl.create()'
      autocomplete='off'
      )
        .col-md-12.col-sm-12.col-xs-12.no-padder
          .row
            label.col-xs-12.input-group
              | Enter Store
              span.text-danger *
              input.form.form-control(
              type='text'
              name='name'
              placeholder='eg. flipkart.com'
              ng-model='$ctrl.data.name'
              autofocus
              required
              )
              span.wrapper-xs.text-xs.text-danger(ng-show='$ctrl.clickUpload && newStoreForm.name.$dirty && newStoreForm.name.$invalid') Please Enter Store

          .modal-footer
            button.btn.btn-success(type='submit' ng-disabled="$ctrl.submitting")
              span.text-white(ng-if='!$ctrl.submitting') Save Store
              span(ng-if='$ctrl.submitting')
                i.fa.fa-spin.fa-circle-o-notch
                span.text-white &nbsp;&nbsp;Submitting...
