.modal-header
  .modal-title
    .h4.text-info View Photos
      .close(ng-click='$ctrl.$uibModalInstance.dismiss("close")') ×

div.m-t-xl.no-margin
  center.hbox
    img(ng-src='{{$ctrl.URLS.CDN + "/shoppre/" + $ctrl.photoUrl}}', height='500', width='500')
